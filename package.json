{
  "name": "gemini-multilingual-kamus",
  "version": "1.0.0",
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.26",
    "@types/react-dom": "^18.3.7",
    "@cloudflare/workers-types": "^4.20241022.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.9.3",
    "wrangler": "^3.114.15"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.html manifest.json sw.js dist/ && esbuild index.tsx --bundle --outfile=dist/bundle.js --loader:.tsx=tsx --define:process.env.NODE_ENV='\"production\"'",
    "dev": "wrangler pages dev dist --port 8788"
  }
}
